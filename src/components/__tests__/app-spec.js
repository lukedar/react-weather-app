import React from 'react';
import ReactDOM from 'react-dom';
import TestUtils from 'react-addons-test-utils';

describe('App', () => {
  beforeEach(() => {
  });

  afterEach(() => {
  });

  describe('Render', () =>  {
    it('should render a searchBar and WeatherCard component', () =>  {
			// let searchBar = TestUtils.findRenderedComponentWithType(App, searchBar);
			// let weatherCard = TestUtils.findRenderedComponentWithType(App, searchBar);
	  	//  expect(searchBar).toEqual(1);
	    });
  });
});